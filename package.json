{
  "name": "@dreamcats/opencommands",
  "version": "2.0.0",
  "description": "Simplified command loader for AI coding agents",
  "type": "module",
  "bin": {
    "opencommands": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "node --test",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.1.0",
    "chalk": "^5.3.0",
    "commander": "^11.1.0",
    "ora": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsup": "^8.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
